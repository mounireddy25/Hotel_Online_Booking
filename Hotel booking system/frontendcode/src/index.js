import React from 'react';
import ReactDOM from 'react-dom';
// Comment out or remove this line if you don't need index.css
// import './index.css';  // Optional styling import

import App from './App';  // Make sure App is imported correctly

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
